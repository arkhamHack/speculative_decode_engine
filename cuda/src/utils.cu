// utils.cu -- Placeholder compilation unit.
// All device math utilities are inlined in include/utils.h for use in both
// multi-kernel and megakernel paths without link-time complications.
#include "utils.h"
